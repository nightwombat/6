#include <stdio.h> 
#include <direct.h> 
 
int main() { 
    if (mkdir("KP_Gpafika_Morozov.") == 0){ 
        printf("Folder created\n"); 
    } else { 
        puts("Error"); 
    } 
    return 0; 
}
